# --- Servidor y Protocolo (Optimizado para HTTP/2 en Cloud Run) ---
fastapi
uvicorn[standard]
gunicorn
h2

# --- SDK Nativo de Google Cloud (Sin LangChain) ---
# Forzamos la versión estable que soporta labels y Gemini 2.0
google-cloud-aiplatform==1.71.1
google-cloud-storage>=2.14.0

# --- Procesamiento de Datos y Archivos ---
# Necesario para la Matriz Marcus y procesamiento de imágenes
pandas
openpyxl
Pillow
numpy

# --- Validaciones y Utilidades ---
pydantic
pyyaml
python-dotenv
python-multipart

# --- Base de Datos (Si persiste la conexión) ---
psycopg2-binary